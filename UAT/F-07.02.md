# UAT — F-07.02 Executive Overview

Story: `S-07.02.01`

Status: **UAT_PENDING**. Repository implementation is not production acceptance.

## Preconditions

- `S-05.01.01` has local + Render verification recorded as passing.
- `S-02.04`, `S-05.02`, `S-04.02`, `S-06.02` and `S-07.01` have satisfied their required executable gates in dependency order.
- PostgreSQL migrations are at head.
- A production-like staging organisation has at least two projects, structured progress, confirmed/candidate memory, AI requests/cost records, at least one budget and external API usage observations.
- Use at least two users with different restricted-project access.
- Use the official WorkOS frontend path for browser UAT; do not hardcode a browser bearer token.

Record:

- Date/timezone:
- Environment URL:
- Backend commit SHA:
- Migration head:
- Tester:
- Executive user IDs/roles:
- Restricted project fixture IDs:

## UAT-1 Executive permission gate

1. Call `/api/v1/organizations/{org}/executive-overview` as an Executive/Owner/Admin with `audit.read`.
2. Repeat as a Member/Manager without `audit.read`.
3. Repeat with a valid user from a different organisation.

Pass only if:

- audit-authorised user succeeds;
- role without `audit.read` receives 403;
- cross-tenant user receives no organisation data;
- no secret/token/credential material appears in response or logs.

Result:

## UAT-2 Permission-aware project portfolio

Create one organisation-visible project and one restricted project. Give only User A access to the restricted project.

Pass only if:

- User A sees both projects;
- User B sees only the organisation-visible project;
- User B receives no restricted project name, node ID, progress, evidence count, blocker/decision summary, source metadata or work-graph-scoped budget target ID;
- Executive role by itself does not bypass resource access.

Leakage failures: ___

## UAT-3 Deterministic progress

Configure visible project work with known weights/states. Independently calculate:

`progress = 100 * sum(weight where state=done) / sum(all configured visible weights)`

Pass only if:

- API percentage matches the independent calculation exactly to the documented rounding precision;
- an unconfigured project returns `progress_percent=null` rather than 0 or an estimate;
- top-level `blocked`, `in_progress`, `done`, `not_started` and `unconfigured` counts sum to `visible_project_count`;
- removing a project/work-item graph link prevents the stale progress item from contributing;
- changing message volume, evidence count or AI output does not alter percentage.

Result:

## UAT-4 Active blockers and confirmed decisions

Use project evidence containing candidate decisions/blockers, then exercise the S-04.02 human-review flow.

Pass only if:

- machine-only candidates never appear in organisation-level `active_blockers` or `confirmed_decisions`;
- a human-confirmed blocker appears with canonical/search/work-graph source IDs;
- a human-confirmed decision appears with source IDs;
- one memory linked to multiple visible projects is deduplicated at organisation level while retaining all visible project links;
- reject/resolve/revoke transitions are reflected on the next read according to S-04.02/S-07.01 rules.

Result:

## UAT-5 AI spend reconciliation

For the current UTC month, select a bounded set of successful AI request IDs and independently recompute their request-scoped costs using the accepted S-06.02 rate cards, including cached-input pricing.

Pass only if:

- executive `known_spend_nano_usd` equals the sum of calculated cost records in period;
- provider breakdown sums to the organisation total;
- input/cached-input/output token totals reconcile to request records;
- a successful request with unknown cost increments `unknown_cost_requests` and makes `cost_complete=false`;
- unknown cost is never treated as zero for completeness.

Result:

## UAT-6 Budget warnings

Create/test at least:

- organisation budget below warning threshold;
- organisation budget above warning threshold;
- exhausted budget;
- budget with unknown-cost requests;
- Work-Graph-scoped budget for a project hidden from User B.

Pass only if:

- below-threshold complete budget is absent from warnings;
- threshold/exhaustion math matches independent known-spend / limit calculation;
- unknown cost makes enforcement incomplete;
- hidden Work-Graph budget is absent for the unauthorised user, including its target ID;
- hard-limit/warning metadata exactly matches the policy.

Result:

## UAT-7 External API usage and cost honesty

Reconcile the executive service usage counts against trusted `APIUsageObservation` rows for the current month.

Pass only if:

- observation/success/failure counts match the source records;
- service breakdown reconciles to the total;
- expired/revoked grants are not counted as currently active grants;
- no credential value or secret reference is returned;
- `known_spend_nano_usd` remains `null` and `cost_status=not_modeled` while no reviewed API tariff/cost ledger exists.

Do **not** accept any implementation that estimates API money from call count or latency.

Result:

## UAT-8 Risk rules

Pass only if each displayed risk can be independently reproduced from deterministic source state:

- blocked project -> S-07.01 blocked state;
- budget warning/exhaustion -> budget snapshot math;
- incomplete AI cost -> successful unknown-cost request exists;
- API cost visibility gap -> API usage exists and monetary API cost model does not.

Confirm there is no generic AI-created executive risk score.

Result:

## UAT-9 Metric provenance

For a sample of every metric family, follow the supplied source IDs/drill-down contract and independently verify the value.

Required families:

- project/status/progress;
- blocker count/details;
- decision count/details;
- AI spend/tokens/provider breakdown;
- budget warning;
- external API usage;
- external API monetary-cost unavailability.

Pass only if provenance points to a real current source contract and no dead/nonexistent drill-down path is advertised.

Result:

## UAT-10 No employee productivity score

Inspect the API schema, response and frontend.

Pass only if:

- there is no `employee_productivity_score`, `productivity_score`, employee-worth or equivalent scoring field;
- there is no employee worth/productivity/performance ranking;
- message count, commit count, token spend, latency or activity volume is not converted into employee quality/value;
- user-level cost attribution, where visible for governance, is not labelled or rendered as performance.

Result:

## UAT-11 Frontend/manual/accessibility

Through the official authenticated frontend verify:

- project portfolio and status basis are understandable;
- blockers/decisions have accessible evidence drill-downs;
- incomplete AI cost is visually distinct from $0 spend;
- API monetary cost `not_modeled` is visually distinct from $0;
- budget warnings communicate threshold/exhaustion/incomplete enforcement;
- restricted projects disappear immediately after access revocation;
- keyboard navigation, visible focus, screen-reader labels and loading/error states work;
- no access token, API key or secret reference is serialized into client configuration or displayed in the UI.

Result:

## Acceptance execution note — 2026-09-11

Acceptance execution has started. The latest GitHub Backend CI, Delivery Verifier and Release Gate runs all concluded `failure`, but all corresponding job step lists were empty. No checkout, install, Ruff, pytest, delivery verifier, migration, backup/restore, image build or smoke step executed. Treat these runs as runner/platform-start failures only; they are neither feature failures nor passing evidence.

## Final acceptance

Backend executable verification: `PENDING`  
Permission/revocation UAT: `PENDING`  
AI cost/budget reconciliation: `PENDING`  
External API reconciliation: `PENDING`  
Frontend/manual/accessibility UAT: `PENDING`  
Feature PASSED: **NO — until all required evidence is executed and recorded.**
