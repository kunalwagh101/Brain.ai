# F-10.08 Workspace Admin & Governance UAT

Story: `S-10.08.01`

Status: implementation in progress. No PASS is claimed until executable backend checks plus authenticated browser UAT complete.

## Scope

The Brain workspace provides an Owner/Admin-only administration surface for:

- organisation members and roles;
- integration lifecycle/health;
- AI provider and model governance;
- external API service/grant ownership, scopes, expiry and usage state.

The frontend must use authoritative backend permission checks. Browser code must never receive stored credential values, secret references, provider tokens or reusable Brain bearer tokens.

## Backend acceptance

1. Owner can read `/api/v1/organizations/{organization_id}/admin-center`.
2. Admin can read the same aggregate.
3. Manager/member/executive/guest cannot read the privileged aggregate unless their backend permissions are explicitly changed in a reviewed policy update.
4. Cross-tenant access returns no target organisation data.
5. Response includes safe member identity/role, integration status/health, AI provider/model status and API grant governance metadata.
6. Response never serializes `secret_ref`, raw credential dictionaries, API keys, OAuth tokens or integration external-account secrets.
7. `credential_present` is boolean metadata only.
8. Counts reconcile exactly with the visible aggregate rows.
9. Owner can assign Owner/Admin and regular roles.
10. Admin can manage regular roles but cannot assign, demote or remove Owner/Admin memberships.
11. The final Owner cannot be demoted or removed; both mutations must return `409`.
12. Removing an organisation membership deletes that user's organisation `ResourceGrant` rows and revokes active restricted-channel memberships so re-adding the user cannot silently restore stale access.
13. Membership create/role/remove mutations produce security-audit evidence.
14. Integration revocation must transition through the existing secret-deletion lifecycle and never return stored credentials.
15. AI provider/model enable-disable remains enforced by `ai.manage`; provider revocation deletes the stored provider credential through the existing gateway lifecycle.
16. API grant enable-disable/revoke remains enforced by `api.manage`; revocation removes the stored grant credential through the existing registry lifecycle.

## Frontend acceptance

1. Owner/Admin sees `Admin & governance` in the Brain workspace navigation.
2. Other roles do not trigger the privileged Admin Center backend request and do not see the navigation item.
3. Member/role list matches the backend aggregate.
4. Owner sees Owner/Admin choices; Admin cannot use the UI to mutate protected Owner/Admin memberships.
5. Add-member, role-change and remove-member actions use only the same-origin Admin Center route.
6. Destructive member removal requires explicit browser confirmation.
7. Integration health and error state match backend data, and integration revoke requires explicit confirmation.
8. AI provider/model enabled/disabled state matches backend data; provider revocation requires explicit confirmation.
9. API grant owner, environment, scopes, expiry, credential-present state and usage count match backend data.
10. API grant enable-disable and revoke use the same-origin route; revoke requires explicit confirmation.
11. No secret value is visible in rendered HTML, browser network responses or client JavaScript state.
12. Browser admin code never constructs an `Authorization` header or receives a reusable Brain/WorkOS bearer token.
13. Admin Center remains usable on desktop and narrow/mobile layouts.
14. Keyboard navigation and focus order cover Admin navigation plus all member/integration/AI/API mutation controls.
15. After every successful mutation, the UI refreshes from the authoritative server response rather than relying on an optimistic local permission state.

## Same-origin BFF acceptance

The reviewed WorkOS activation must create exactly one Admin Center mutation endpoint:

`POST /api/brain/organizations/{organization_id}/admin-center/actions`

The route must:

- require a real WorkOS `withAuth()` session and server-side access token;
- enforce `application/json` and an 8 KiB request ceiling;
- accept only the reviewed discriminated action contract;
- revalidate current Brain organisation membership and Owner/Admin role before forwarding;
- keep raw FastAPI/provider/secret-store error details out of browser responses;
- preserve safe upstream HTTP statuses only;
- never serialize stored credentials, secret references or tokens.

Current allow-listed actions are:

- `member_invite`;
- `member_role`;
- `member_remove`;
- `integration_revoke`;
- `ai_provider_status`;
- `ai_provider_revoke`;
- `ai_model_status`;
- `api_grant_status`;
- `api_grant_revoke`.

Credential creation/rotation remains a separate future secret-entry flow and must not be folded into ordinary status toggles without dedicated validation and UAT.

## Required executable evidence

- Ruff for `backend/app/routes/admin_center.py`, `backend/app/routes/organizations.py` and related tests.
- Pytest including `backend/tests/test_admin_center.py` and `backend/tests/test_membership_governance.py`.
- Frontend lint/build/source-contract tests including `tests/admin-center-contract.test.mjs`.
- PostgreSQL-backed acceptance for aggregate/member mutation queries.
- Authenticated WorkOS browser UAT with at least Owner, Admin and Member accounts.
- Network/browser inspection proving no secret serialization.
- Re-add-a-removed-user test proving stale restricted/resource grants do not reactivate.

Until those pass, keep `S-10.08.01` out of DONE/PASSED.
