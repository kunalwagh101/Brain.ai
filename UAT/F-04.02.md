# UAT — F-04.02 Decision and Blocker Memory

Status: `UAT_PENDING`

Engineering implementation is not user acceptance. Do not mark this feature PASSED until the checks below are executed against a realistic backend dataset and the frontend/manual workflow is inspected.

## Environment record

- Date:
- Environment:
- Backend commit SHA:
- Database/migration revision:
- Tester:
- Search/Increment 8 verification status:
- Frontend build/commit:

## Required dataset

Use authorised test/company data containing, at minimum:

- one public Slack channel with explicit authorisation;
- one private Slack channel with at least two members and one non-member;
- one public GitHub repository;
- one private/internal GitHub repository with explicit grants;
- explicit decisions, explicit blockers, ordinary status updates, tentative ideas and resolved blockers;
- at least 30 labelled decision examples, 30 labelled blocker examples and 30 hard negatives before using the result as a production precision claim.

Do not use shared customer credentials. Test accounts/workspaces/repos must be dedicated or explicitly approved.

## Backend checks

### 1. Migration and worker

1. Apply migrations through `20260907_0010` on PostgreSQL.
2. Run the search projection worker so the selected evidence has `SearchDocument` rows.
3. Run:

```bash
cd backend
python -m app.decision_memory_worker --once --batch-size 100
```

Record processed documents, created candidates and remaining documents. Run it again and confirm unchanged documents do not create duplicates.

Result:

### 2. Machine state safety

For every newly extracted machine record, confirm:

- state is `candidate`, never `confirmed`;
- kind is only `decision` or `blocker`;
- confidence is between 0 and 1;
- canonical/search/work-graph evidence identifiers are present as applicable;
- source provenance points back to the correct evidence.

Result:

### 3. Tenant isolation

As a user from Org A, attempt to list/fetch known Org B candidate IDs. Confirm no Org B content, title, excerpt, provenance or review history is returned.

Leakage failures: ___

### 4. Public Slack current authorisation

1. Confirm an authorised public channel candidate is visible.
2. Remove the Brain Slack channel authorisation.
3. Query memory again without re-extracting.
4. Confirm the candidate disappears.

Result:

### 5. Private Slack current membership

1. Confirm a current private-channel member can see the candidate.
2. Confirm a non-member cannot.
3. Remove the member from current channel membership/authorisation.
4. Confirm the candidate disappears immediately without deleting historical source ACL provenance.

Result:

### 6. Private/internal GitHub grant

1. Confirm Owner/Admin without an explicit matching restricted-resource grant cannot read the candidate.
2. Add the approved grant and confirm the candidate becomes visible.
3. Revoke the grant and confirm it disappears immediately.

Result:

### 7. Integration/source revocation

1. Revoke the integration connection and confirm its candidates disappear from reads.
2. Restore only in an approved test environment.
3. Mark/delete a source through the real source ingestion path and confirm the derived candidate is no longer returned.
4. Confirm raw/canonical evidence follows its separate retention policy and review rows were not silently rewritten.

Result:

### 8. Human review transitions

Exercise:

- candidate -> confirmed;
- candidate/confirmed -> rejected;
- confirmed blocker -> resolved;
- rejected -> candidate via reopen;
- resolved blocker -> confirmed via reopen;
- edit on an allowed state.

Confirm each mutation records actor, action, reason, previous/new state and previous/new summary. Confirm a decision cannot be resolved and invalid transitions fail without partial mutation.

Result:

### 9. Re-extraction

Change/rebuild one approved test evidence projection and rerun extraction. Confirm:

- same statement does not duplicate;
- a no-longer-emitted unreviewed candidate becomes `superseded`;
- human-reviewed state/edited summary is not silently overwritten;
- zero-candidate documents do not get reprocessed forever.

Result:

## Labelled precision evaluation

Create a reviewer-labelled evaluation sheet before looking at extractor output.

For each item record:

- source ID;
- expected kind: decision / blocker / neither;
- expected explicit statement;
- extractor emitted: yes/no;
- emitted kind;
- true positive / false positive / false negative;
- reviewer notes.

Calculate separately:

- Decision precision = TP_decision / (TP_decision + FP_decision)
- Blocker precision = TP_blocker / (TP_blocker + FP_blocker)
- Optional recall = TP / (TP + FN), reported separately and not substituted for precision.

Decision precision: ___  
Blocker precision: ___  
False positives reviewed: ___

Acceptance target: **>=90% precision for both agreed candidate classes on the representative labelled set before production claim.** The synthetic pytest fixture is not sufficient evidence for this gate.

## Frontend/manual UAT

The production frontend must eventually allow an authorised user to:

1. see decision/blocker candidates with clear `candidate` vs `confirmed` state;
2. open the cited source/evidence context;
3. confirm/reject/edit a candidate with a reason;
4. resolve/reopen blockers;
5. see review history appropriate to their permissions;
6. lose access immediately when underlying source access is revoked.

Current preview/sample frontend state must not be used to claim this check passed.

Frontend result: `PENDING`

## Rollback check

On a disposable database/environment:

1. export review history if required;
2. downgrade `20260907_0010 -> 20260907_0009`;
3. confirm decision-memory tables are removed;
4. confirm raw/canonical/search/work-graph evidence remains intact;
5. re-upgrade and reconcile to prove machine candidates are rebuildable.

Result:

## Final acceptance

Backend realistic-data UAT: `PENDING`  
Labelled precision gate: `PENDING`  
Frontend/manual UAT: `PENDING`  
Feature PASSED: **NO — until all required evidence above is recorded.**
