# F-10.06 Conversation UX UAT

Story: **S-10.06.01 — Make Brain channels feel like Slack/Discord with safe conversation affordances**  
Engineering verdict: **DONE**. Product acceptance: **UAT_PENDING**.

## Automated checks already executed locally

Date: 2026-09-13

```bash
cd backend
../.venv/bin/ruff check app tests migrations
../.venv/bin/pytest -q
# All checks passed; 299 passed

../.venv/bin/pytest -q tests/test_native_chat.py tests/test_native_conversation.py tests/test_observability.py
# 24 passed

cd ..
npm run lint
npm test
# Build complete; 12 passed, 1 intentionally skipped

node --test tests/workspace-contract.test.mjs
# 11 passed

cd backend
../.venv/bin/alembic upgrade head --sql
# PostgreSQL SQL generated through revision 20260912_0020
```

These checks cover service/API tenant and revocation boundaries, thread placement, exact mention deduplication, reaction allow-list/idempotency/aggregates, numeric unread ordering, agent distinction contracts, same-origin/no-browser-token contracts and frontend compilation. Offline SQL compilation does not prove a live migration.

## Required environment

- Disposable PostgreSQL 17 with pgvector and the current branch migration chain.
- Official S-10.04 WorkOS AuthKit packages installed and activated with a real lockfile.
- Two active users in organisation A, one active user in organisation B.
- One organisation-visible channel and one restricted channel in organisation A.
- Desktop browser plus a narrow/mobile viewport; keyboard-only navigation available.

Record environment URL, commit SHA, migration revision, tester, UTC start/end and screenshots/video in the result section. Do not paste tokens, cookies, message bodies from restricted channels or secrets.

## Live PostgreSQL migration and integrity

1. Back up the disposable database, run `alembic upgrade head`, confirm revision `20260912_0020`, then run the focused backend tests with `BRAIN_TEST_DATABASE_URL` set.
2. Confirm existing pre-migration native messages received increasing per-channel sequences and each channel counter equals its maximum sequence.
3. In a transaction, attempt a thread/mention/reaction/read row whose organisation or channel does not match its message. Each insert must fail by database constraint.
4. Stop writes, downgrade to `20260912_0019`, confirm conversation tables/columns are removed while existing root messages and evidence remain, then upgrade to head again.

Expected: migration, constraints, downgrade and forward recovery pass with no root-message/evidence loss.

## Authenticated conversation path

1. Sign in as organisation A user 1. Create/open a visible channel and send two roots quickly.
2. Sign in as user 2. Confirm unread is exactly 2. Open the channel and confirm it becomes 0 without changing user 1's state.
3. Open a root thread, send a reply, reload, and confirm it remains under that root only. Try to use the reply ID as a root and confirm rejection without content leakage.
4. Send `@exact-user-2-email` twice plus an unknown and an organisation-B email. Confirm only user 2 is highlighted/resolved once; no unknown/cross-tenant identity appears.
5. User 1 adds the same allowed reaction twice; confirm count is 1. User 2 adds it; confirm count is 2. Remove user 2's reaction; confirm count is 1. Confirm unsupported values are rejected.
6. Create an agent-authored message through the governed agent path. Confirm the UI shows the named agent and Agent label distinctly from a human.

Expected: every result matches the server after a reload and no client-generated identity/aggregate is treated as authority.

## Revocation and cross-tenant privacy

1. Add user 2 to the restricted channel, create a root, exact mention, reply, reaction and unread state, then revoke the membership.
2. As user 2, request the channel list, root list, thread, reaction mutation and read mutation. Inspect UI and network responses.
3. As organisation B's user, repeat calls using organisation A channel/message IDs.

Expected: the restricted channel disappears from summaries/navigation; content, member names, mention identities, reaction counts and unread state do not appear; direct object requests fail with safe 404/403 responses.

## Browser, accessibility and responsive checks

1. Confirm the selected channel appears before company cards and the channel navigation has accurate unread badges.
2. Navigate channel links, reaction toggles, thread open/close, provenance disclosure and composers using keyboard only. Confirm visible focus and meaningful accessible names.
3. Open a thread and press Escape. Confirm focus moves to the thread heading on open and the pane closes without trapping keyboard focus.
4. Test desktop, 900 px, 720 px and 375 px widths. Confirm thread context stacks, text wraps and no horizontal page overflow hides controls.
5. Check empty, loading, read-only, session-expired, permission-denied, not-found, conflict, rate-limit and generic failure states. Confirm no arbitrary backend/provider detail or token appears.
6. Inspect browser storage and network requests. Confirm no Brain bearer token is present and cross-site mutation requests are rejected.

## Result ledger

| Gate | Result | Evidence |
|---|---|---|
| Repository-wide backend Ruff/Pytest | PASS | Release Gate 35789299001: Ruff clean + 385 passed, 2 warnings |
| Conversation regressions | PASS | Included in the 385-test Release Gate suite on code commit `10a9028685f67ec6db923d3371a67df13276d044` |
| Frontend build/source contracts | PASS | Release Gate 35789299001: production build complete; 138 passed, 0 failed |
| Delivery verifier | PASS | Workflow 35789298922 completed successfully on the same code head |
| Live PostgreSQL upgrade/downgrade/recovery | PASS | Release Gate 35789299001 upgraded to head, downgraded to base and upgraded to head again |
| Official WorkOS activation | PENDING | S-10.04 dependency |
| Authenticated multi-user browser path | PENDING | required |
| Revocation/cross-tenant browser path | PENDING | required |
| Keyboard/responsive/accessibility | PENDING | required |

Engineering state: **DONE**. Final product acceptance remains **UAT_PENDING** until official WorkOS activation plus authenticated multi-user, revocation, responsive and accessibility checks pass.
